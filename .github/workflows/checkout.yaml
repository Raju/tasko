name: checkout

on:
  push:
    branches:
      - main
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.0
      - name: hello world
        run: echo "Hello World!"
        shell: bash
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: good bye
        run: echo "Good bye!"
        shell: bash
